# Smoke Test Checklist

Supabase実データで、おかね会議MVPの主要フローを最後まで確認するためのチェックリストです。Stripe、課金、PWA、通知、AIチャットは今回の対象外です。

## 事前準備

- [ ] `npm install` が完了している
- [ ] `.env.local` にSupabaseの認証情報を設定している
- [ ] `NEXT_PUBLIC_SITE_URL=http://localhost:3001` を設定している
- [ ] Supabase SQL Editorで `supabase/schema.sql` を実行済み
- [ ] 既存DBの場合、`child_profiles` の `age_group` 制約に `age_3_5` / `age_12_15` が追加されている
- [ ] Supabase Authのメール確認がONになっている
- [ ] SupabaseのSite URLが `http://localhost:3001` になっている
- [ ] Redirect URLsに `http://localhost:3001/**` と `http://127.0.0.1:3001/**` を追加している
- [ ] Confirm signupメールテンプレートが `/auth/confirm` を通るリンクになっている

## 認証

- [ ] `/signup` で親アカウントを作成できる
- [ ] 登録ボタンは、メール、パスワード、確認用パスワード、同意が揃うまで押せない
- [ ] 弱いパスワードでは、8文字以上かつ英字と数字が必要だと分かるエラーになる
- [ ] パスワード確認が一致しない場合、メールだけ保持され、パスワード欄は空に戻る
- [ ] signup後に `/signup/check-email?email=...` へ進む
- [ ] `/signup/check-email?email=...` ではメールが固定表示され、編集可能なメール入力欄がない
- [ ] 確認メールリンクから `/setup/child` へ進む
- [ ] 親がlogin/logoutできる
- [ ] 未ログイン時に保護ページへ入れない

## 初回設定と複数子ども

- [ ] 子どもプロフィールを作成できる
- [ ] ニックネームは30文字まで
- [ ] 年齢グループに `3〜5歳`, `6〜8歳`, `9〜12歳`, `12〜15歳`, `その他` がある
- [ ] お金の単位は `円` と `ポイント` から選べる
- [ ] 最初にあるお金にマイナスを登録できない
- [ ] PINは任意で、未入力でも作成できる
- [ ] PINを入力する場合は4けた数字だけ受け付ける
- [ ] 入力エラー時、PIN以外の入力内容が保持される
- [ ] `作成してもうひとり追加` で続けて2人目を作成できる
- [ ] `作成してダッシュボードへ` で `/parent/dashboard` へ進む
- [ ] walletが子どもごとに自動作成される
- [ ] 初期残高が子どもごとのwalletに反映される

## Parent Mode

- [ ] `/parent/dashboard` に全ての子どもカードが表示される
- [ ] 各子どもカードに正しい残高が表示される
- [ ] 各子どもの `子ども画面を開く` が正しい子どもIDへ進む
- [ ] PINありの子どもはPIN画面へ進む
- [ ] PINなしの子どもはそのままChild Modeへ進む
- [ ] 未対応相談件数が正しく表示される
- [ ] `/parent/consultations` に相談が表示される
- [ ] 相談一覧フィルターで横スクロールバーが出ない
- [ ] `/parent/consultations/[id]` で相談詳細を確認できる
- [ ] 親がコメントと判断を保存できる

## Child Mode

- [ ] `/child/select` で子どもプロフィールを選べる
- [ ] `/child/[childId]/home` で今あるお金が表示される
- [ ] 子どもがほしいものを登録できる
- [ ] ほしいもの登録のカテゴリ名が重複表示されない
- [ ] `その他` カテゴリを選べる
- [ ] `くわしく書く（任意）` を入力できる
- [ ] `どれくらいほしい？` の星に意味が表示される
- [ ] 金額が今あるお金を超えると警告が出る
- [ ] 金額超過の警告が出ても次へ進める
- [ ] 登録後に買う前チェックへ進む
- [ ] 買う前チェックに回答できる
- [ ] 買う前チェックの相談ボタンが `おとうさん・おかあさんに相談する` と表示される
- [ ] 買う前チェック送信後、相談が1件作成される
- [ ] 同じほしいもので相談が重複作成されない
- [ ] 親判断後、子ども結果画面に判断とメッセージが反映される

## Wallet

- [ ] `/parent/wallet` で現在残高が子どもごとに表示される
- [ ] 親がwallet残高を追加できる
- [ ] 親がwallet残高を減少できる
- [ ] 残高不足の減少は分かりやすいエラーになる
- [ ] お金のきろくが表示される
- [ ] OK後に購入済みにするとwallet残高が減る
- [ ] 購入済み処理後、wish itemの状態が「買ったもの」になる

## データ安全性

- [ ] 別親ユーザーのデータがRLSで見えない
- [ ] 子どもの本名、学校名、住所、生年月日を入力させていない
- [ ] `SUPABASE_SERVICE_ROLE_KEY` がクライアントコードに露出していない

## UI確認

- [ ] 360px幅で主要フローが操作できる
- [ ] 390px幅で主要フローが操作できる
- [ ] 430px幅で主要フローが操作できる
- [ ] Empty Stateが自然な日本語で表示される
- [ ] エラー表示が操作可能な内容になっている

## コマンド確認

- [ ] `npm run lint`
- [ ] `npm run build`
